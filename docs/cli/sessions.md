---
summary: "CLI reference for `ws-agent sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `ws-agent sessions`

List stored conversation sessions.

```bash
ws-agent sessions
ws-agent sessions --active 120
ws-agent sessions --json
```
