---
summary: "CLI reference for `ws-agent uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `ws-agent uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
ws-agent uninstall
ws-agent uninstall --all --yes
ws-agent uninstall --dry-run
```
