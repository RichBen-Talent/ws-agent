services:
  ws-agent-gateway:
    image: ${WS_AGENT_IMAGE:-ws-agent:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      WS_AGENT_GATEWAY_TOKEN: ${WS_AGENT_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${WS_AGENT_CONFIG_DIR}:/home/node/.ws-agent
      - ${WS_AGENT_WORKSPACE_DIR}:/home/node/.ws-agent/workspace
    ports:
      - "${WS_AGENT_GATEWAY_PORT:-18789}:18789"
      - "${WS_AGENT_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${WS_AGENT_GATEWAY_BIND:-lan}",
        "--port",
        "18789",
      ]

  ws-agent-cli:
    image: ${WS_AGENT_IMAGE:-ws-agent:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      WS_AGENT_GATEWAY_TOKEN: ${WS_AGENT_GATEWAY_TOKEN}
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${WS_AGENT_CONFIG_DIR}:/home/node/.ws-agent
      - ${WS_AGENT_WORKSPACE_DIR}:/home/node/.ws-agent/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
